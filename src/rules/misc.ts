import type { StaticRule } from '@unocss/core'

export const misc: StaticRule[] = [
  ['over-fill', {
    position: 'absolute',
    top: 0,
    left: 0,
    width: '100%',
    height: '100%',
  }],
]
